{
  "name": "refrr",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "start": "cd frontend && node server.js",
    "build": "cd frontend && npm install --production=false --no-optional --no-audit --no-fund && NODE_OPTIONS='--max-old-space-size=256 --max-semi-space-size=64' npm run build:prod",
    "install": "cd frontend && npm install --production=false --no-optional --no-audit --no-fund"
  }
}
