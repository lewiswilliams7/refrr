import React from 'react';
import { Container, Typography } from '@mui/material';

const Referrals: React.FC = () => {
  return (
    <Container>
      <Typography variant="h4" component="h1" gutterBottom>
        Referrals
      </Typography>
      {/* Add referrals management UI here */}
    </Container>
  );
};

export default Referrals; 